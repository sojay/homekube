---
nodes:
  - name: "talos-controlplane"            # (REQUIRED) Name of the node (must match [a-z0-9-\]+)
    address: "192.168.0.234/23"         # (REQUIRED) IP address of the node (must be in the node_cidr)
    controller: true    # (REQUIRED) Set to true if this is a controller node
    disk: "/dev/sda"            # (REQUIRED) Device path or serial number of the disk for this node (talosctl get disks -n <ip> --insecure)
    mac_addr: "bc:24:11:d5:f1:80"        # (REQUIRED) MAC address of the NIC for this node (talosctl get links -n <ip> --insecure)
    schematic_id: "ce4c980550dd2ab1b17bbf2b08801c7eb59418eafe8f279833297925d67c7515"    # (REQUIRED) Schematic ID from https://factory.talos.dev/
    mtu: 1500           # (ADVANCED/OPTIONAL) MTU for the NIC. DEFAULT: 1500
    secureboot: false   # (ADVANCED/OPTIONAL) SecureBoot mode on UEFI platforms. Ref: https://www.talos.dev/latest/talos-guides/install/bare-metal-platforms/secureboot
    encrypt_disk: false # (ADVANCED/OPTIONAL) TPM-based disk encryption. Ref: https://www.talos.dev/latest/talos-guides/install/bare-metal-platforms/secureboot

  - name: "talos-worker"            # (REQUIRED) Name of the node (must match [a-z0-9-\]+)
    address: "192.168.0.235/23"         # (REQUIRED) IP address of the node (must be in the node_cidr)
    controller: true    # (REQUIRED) Set to true if this is a controller node
    disk: "/dev/sda"            # (REQUIRED) Device path or serial number of the disk for this node (talosctl get disks -n <ip> --insecure)
    mac_addr: "bc:24:11:a2:3e:24"        # (REQUIRED) MAC address of the NIC for this node (talosctl get links -n <ip> --insecure)
    schematic_id: "ce4c980550dd2ab1b17bbf2b08801c7eb59418eafe8f279833297925d67c7515"    # (REQUIRED) Schematic ID from https://factory.talos.dev/
    mtu: 1500           # (ADVANCED/OPTIONAL) MTU for the NIC. DEFAULT: 1500
    secureboot: false   # (ADVANCED/OPTIONAL) SecureBoot mode on UEFI platforms. Ref: https://www.talos.dev/latest/talos-guides/install/bare-metal-platforms/secureboot
    encrypt_disk: false # (ADVANCED/OPTIONAL) TPM-based disk encryption. Ref: https://www.talos.dev/latest/talos-guides/install/bare-metal-platforms/secureboot


